# periodic-sentence-detection
STAGE PLANS
>Python script that extracts sentence stops (commas, full stops etc.)
>Quantify what defines a periodic sentence into variables (number of verbs, nouns, etc.)
